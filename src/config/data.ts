// src/config/data.ts - Market data for personal bid calculation
import { MarketData } from '../types';

const marketData: MarketData = {
  // All complex market data has been removed to simplify the project for Phase 2.
  // This file is now a placeholder for any future configuration needs.
};

export default marketData;